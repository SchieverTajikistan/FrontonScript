meta {
  name: PRINT SHORT SUMMARY
  type: http
  seq: 2
}

post {
  url: {{url}}
  body: json
  auth: inherit
}

body:json {
  {
    "task": "print",
    "data": {
      "actionCode": "2"
    }
  }
}

settings {
  encodeUrl: true
}

docs {
  Напечатать короткий промежуточный отчет о текущем операционном дне (смене).
  
  =====================================================
  Если операционный день закрыт, и после не было транзакций, то ничего не напечатается. 
  =====================================================
}
