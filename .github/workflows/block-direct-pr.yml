name: Block PRs to main

on:
  pull_request:
    branches:
      - main

jobs:
  block:
    runs-on: ubuntu-latest
    steps:
      - name: Block all PRs targeting main
        run: |
          echo "❌ Нельзя отправлять PR напрямую в 'main'. Пожалуйста, откройте ваш PR на 'dev'."
          exit 1